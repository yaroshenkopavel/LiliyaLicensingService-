# Operations

S5.1 is server-neutral and has no production deployment.

Production deployment choices, durable transactional persistence, signing KMS/HSM boundary,
environment separation, rotation, backups and audit retention remain later acceptance gates.
